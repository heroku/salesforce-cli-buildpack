#!/bin/sh

# this pack is valid for apps with a hello.txt in the root
# if [ -f $1/hello.txt ]; then
  # echo "HelloFramework"
  echo "detect waw"

echo "Set path for Salesforce CLI"
export PATH=/tmp/bin/sfdx:$PATH

echo "Update Salesforce CLI"
/tmp/bin/sfdx/sfdx update


  exit 0
# else
  # exit 1
# fi
